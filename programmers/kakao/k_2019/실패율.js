function solution(n, stages) {
  let result = [];
  let clear = new Array(5);
  let arrived = new Array(5);

  for (let i = 0; i < stages.length; i++) {
    stages[i];
  }
  return result;
}

// test
solution(5, [2, 1, 2, 6, 2, 4, 3, 3]); //result [3,4,2,1,5]
// solution(4, [4, 4, 4, 4, 4]); //result 	[4,1,2,3]
